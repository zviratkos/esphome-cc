#include "touch_sensor.h"

namespace esphome {
namespace touch_sensor {

// Implementation is all inline in header for simplicity, so this can be empty

}  // namespace touch_sensor
}  // namespace esphome

